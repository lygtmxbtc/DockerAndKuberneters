# DockerAndKuberneters
Learning Notes
Record the install command for docker and kubernetes
Record the usual command of docker and kubernetes
Record the Dockerfile and Yaml file to build the backend+frontend system
